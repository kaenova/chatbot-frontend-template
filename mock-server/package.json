{
  "name": "chatbot-mock-server",
  "version": "1.0.0",
  "description": "Mock server for chatbot API with SQLite storage",
  "main": "server.ts",
  "scripts": {
    "dev": "bun run server.ts",
    "start": "bun run server.ts"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "bun-types": "^1.0.0"
  }
}